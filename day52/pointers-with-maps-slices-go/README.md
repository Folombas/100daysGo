# 🌟 Day 52/100: Pointers with Maps & Slices — "От новогодней метели к IT-офису"

**Дата**: 25 декабря 2025 г.
**Тема**: Указатели с мапами и слайсами (Pointers with Maps & Slices) в Golang
**Легенда**: Программа, написанная после дня в метель и гололёд: выбор подарков до 3 ночи, два заказа в центр Москвы, решение вернуться домой в 17:00, несмотря на критику подписчиков. Вдохновлена осознанием: «Я — программист, а не курьер». Указатели с мапами и слайсами — как инструмент контроля над своей судьбой.

## 📌 Как это работает
Программа демонстрирует **указатели с мапами и слайсами** через четыре ключевых сценария:
1. `updateOrderStatus(&orders, id, status)` — изменяет статус заказа через **указатель на мапу**
2. `addNewRoute(&routes, route)` — добавляет маршрут через **указатель на слайс**
3. `calculateTotalEarnings(orders, bonus)` — анализирует заработок с **мапой заказов**
4. `updateEnergyLevel(&energy, hours)` — восстанавливает энергию через **указатель на int**

### Ключевые принципы Go:
```go
// Указатель на мапу: изменение статуса заказа
func updateOrderStatus(orders *map[int]Order, orderID int, newStatus string) {
    if order, exists := (*orders)[orderID]; exists {
        order.Status = newStatus
        (*orders)[orderID] = order // Изменяем значение по указателю
    }
}

// Указатель на слайс: добавление маршрута
func addNewRoute(routes *[]string, newRoute string) {
    *routes = append(*routes, newRoute) // Перезаписываем слайс по указателю
}

// Почему это важно?
// Мапы и слайсы в Go передаются как ссылки, но указатели дают КОНТРОЛЬ:
// • Без указателя: копия мапы/слайса не меняет оригинал
// • С указателем: ты управляешь исходными данными — как своей жизнью
