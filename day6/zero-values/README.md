# Day 6: Zero Values Explorer 🎯

## Описание программы
Эта программа демонстрирует концепцию **нулевых значений (Zero Values)** в Go - одно из ключевых преимуществ языка для безопасности и предсказуемости кода.

## Что такое Zero Values?
В Go каждая переменная автоматически получает "нулевое значение" при объявлении, если не задано явное начальное значение. Это предотвращает неопределенное поведение и ошибки.

## Нулевые значения по типам:
- `int`, `float64` → `0`
- `string` → `""` (пустая строка)
- `bool` → `false`
- `slices`, `maps`, `pointers`, `functions` → `nil`
- `structs` → все поля получают свои нулевые значения

## Как запустить:
```bash
cd day6/zero-values
go run main.go
