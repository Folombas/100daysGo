# Day 8: Scope and Shadowing 🎯

## Изучаем области видимости и затенение переменных в Go

Эта программа демонстрирует концепции областей видимости (scope) и затенения (shadowing) переменных в языке Go.

## Что такое Scope (Область видимости)?

Область видимости определяет, где в коде переменная может быть использована. В Go есть несколько уровней:

### Уровни видимости:
1. **Пакетный уровень** - глобальные переменные
2. **Уровень функции** - локальные переменные функции
3. **Блочный уровень** - переменные внутри блоков (if, for, {})

## Что такое Shadowing (Затенение)?

Затенение происходит когда переменная во внутренней области видимости "перекрывает" переменную с тем же именем из внешней области.

## Примеры в программе:

### Глобальные переменные:
```go
var globalVar string = "Я глобальная"
