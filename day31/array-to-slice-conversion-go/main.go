package main

import (
	"fmt"
	"math/rand/v2"
	"strings"
	"time"
)

// Структура для представления состояния Гоши
type Gosha struct {
	Name              string
	Age               int
	PreviousCareer    string
	CurrentJob        string
	ProgrammingGoal   string
	Confidence        float64
	Willpower         float64
	DaysSinceChange   int
	StudyHours        float64
	MoneySaved        float64
	AbandonedGames    int
	AbandonedSeries   int
	DepressionLevel   float64
	HardshipsToday    int
}

// Структура для темы дня
type DailyTheme struct {
	Date   string
	Topic  string
	Day    int
}

// Функция для инициализации Гоши
func initGosha() Gosha {
	return Gosha{
		Name:              "Гоша",
		Age:               38,
		PreviousCareer:    "Рэп-артист",
		CurrentJob:        "Курьер",
		ProgrammingGoal:   "Стать Go-разработчиком",
		Confidence:        33.0,
		Willpower:         48.0,
		DaysSinceChange:   30,
		StudyHours:        3.0,
		MoneySaved:        1150.0,
		AbandonedGames:    14,
		AbandonedSeries:   6,
		DepressionLevel:   80.0,
		HardshipsToday:    5,
	}
}

// Функция для получения текущей темы дня
func getCurrentTheme() DailyTheme {
	return DailyTheme{
		Date:   "4 декабря 2025",
		Topic:  "Slices: Array to Slice Conversion",
		Day:    30,
	}
}

// Функция для генерации мотивационного сообщения
func getMotivationalMessage(r *rand.Rand) string {
	messages := []string{
		"Каждый день без игр — шаг к новой жизни!",
		"Ты не просто кодишь. Ты создаешь будущее!",
		"Твоя история успеха начинается здесь.",
		"Каждая строка кода — это победа над прошлым!",
		"Ты бросил тусовки, теперь ты бросаешь баги!",
		"Сегодня ты конвертируешь массивы в слайсы, а не жизнь в рутину!",
		"Код — это твой новый способ выражения!",
		"Ты не просто программируешь, ты трансформируешься!",
		"Каждый конвертированный массив — это шаг к стабильному будущему!",
		"Ты строишь свою цифровую империю!",
		"Когда мир пытается остановить тебя — конвертируй трудности в возможности!",
		"Твои проблемы — просто массивы, ожидающие преобразования в слайсы успеха!",
	}

	return messages[r.IntN(len(messages))]
}

// Функция для отображения статуса Гоши
func (g Gosha) displayStatus() {
	fmt.Println("🔥🔥🔥 ГОША - ПРОГРАММИСТ-ПЕРЕРОЖДЕННЫЙ 🔥🔥🔥")
	fmt.Println(strings.Repeat("=", 50))
	fmt.Printf("👤 Имя: %s\n", g.Name)
	fmt.Printf("🎂 Возраст: %d лет\n", g.Age)
	fmt.Printf("🎤 Бывшая карьера: %s\n", g.PreviousCareer)
	fmt.Printf("📦 Текущая работа: %s\n", g.CurrentJob)
	fmt.Printf("🎯 Цель: %s\n", g.ProgrammingGoal)
	fmt.Println(strings.Repeat("-", 50))
	fmt.Printf("💪 Уверенность: %.0f%%\n", g.Confidence)
	fmt.Printf("🛡️ Воля: %.0f%%\n", g.Willpower)
	fmt.Printf("📅 Дней с изменениями: %d\n", g.DaysSinceChange)
	fmt.Printf("💻 Часов обучения: %.1f\n", g.StudyHours)
	fmt.Printf("💰 Сэкономлено: %.0f ₽\n", g.MoneySaved)
	fmt.Printf("🎮 Удалено игр: %d\n", g.AbandonedGames)
	fmt.Printf("📺 Удалено сериалов: %d\n", g.AbandonedSeries)
	fmt.Printf("📉 Уровень депрессии: %.0f%%\n", g.DepressionLevel)
	fmt.Printf("💥 Трудностей сегодня: %d\n", g.HardshipsToday)
	fmt.Println(strings.Repeat("=", 50))
}

// Функция для отображения темы дня
func (t DailyTheme) displayTheme() {
	fmt.Printf("📅 Тема дня: %s\n", t.Topic)
	fmt.Printf("📆 Дата: %s\n", t.Date)
	fmt.Printf("🔢 День челленджа: %d\n", t.Day)
	fmt.Println(strings.Repeat("-", 30))
}

// Функция для отображения мотивации
func displayMotivation(r *rand.Rand) {
	fmt.Println("💬 МОТИВАЦИЯ ДНЯ:")
	fmt.Println(getMotivationalMessage(r))
	fmt.Println(strings.Repeat("-", 30))
}

// Функция для отображения истории
func displayHistory() {
	fmt.Println("📖 ИСТОРИЯ ГОШИ СЕГОДНЯ:")
	fmt.Println("Сегодня я замечтался в электричке и уехал в дальний тупик вокзала,")
	fmt.Println("совсем не туда, куда мне нужно было. Потерял пол-дня.")
	fmt.Println("Рано стемнело. Мотался по всему городу в потёмках со своими коробками.")
	fmt.Println("В метро девушка не уступила мне, чтобы я вышел из вагона.")
	fmt.Println("Я её 10 раз спросил, выходит ли она. Хоть она и была в наушниках,")
	fmt.Println("она меня жёстко проигнорировала. Пришлось протискиваться.")
	fmt.Println("На улице холодно. Зашёл в торговый центр погреться.")
	fmt.Println("Посидел за столом на фуд-корте. Охранник выгнал на улицу")
	fmt.Println("потому, что я просто сидел и ничего не заказывал.")
	fmt.Println("А у меня нет денег. Утром пол-зарплаты перевёл маме —")
	fmt.Println("чтобы загладить вину из-за того, что я вчера выложил в Интернете")
	fmt.Println("в Телеграм Сторис её вкусный рыбный супчик с кремом и сыром по-фински.")
	fmt.Println(strings.Repeat("-", 30))
}

// Функция для отображения урока по конвертации массивов в слайсы
func displayConversionLesson() {
	fmt.Println("🔧 Slices: Array to Slice Conversion в GO:")
	fmt.Println("- Массивы в Go имеют фиксированный размер")
	fmt.Println("- Слайсы более гибкие и динамичны")
	fmt.Println("- Конвертация: slice := array[:]")
	fmt.Println("- Можно взять часть массива: slice := array[start:end]")
	fmt.Println("- Слайсы ссылаются на исходный массив")
	fmt.Println("- При изменении слайса меняется и исходный массив")
	fmt.Println("Пример:")
	fmt.Println("  arr := [5]int{1, 2, 3, 4, 5}")
	fmt.Println("  slice := arr[:]  // [1 2 3 4 5]")
	fmt.Println("  slice[0] = 10    // arr[0] тоже станет 10")
	fmt.Println(strings.Repeat("=", 50))
}

// Функция для отображения жизненного урока
func displayLifeLesson() {
	fmt.Println("💭 ЖИЗНЕННЫЙ УРОК ДНЯ:")
	fmt.Println("Когда мир пытается превратить тебя в статичный массив,")
	fmt.Println("научись конвертировать себя в динамический слайс.")
	fmt.Println("Твои трудности сегодня — это не конец истории,")
	fmt.Println("а просто данные, ожидающие преобразования в успех.")
	fmt.Println("Твои хейтеры и те, кто тебя игнорирует,")
	fmt.Println("не определяют твой путь — только ты сам.")
	fmt.Println("И да, даже когда у тебя нет денег на кофе в фуд-корте,")
	fmt.Println("у тебя есть мечта, которая греет лучше любого напитка.")
	fmt.Println(strings.Repeat("-", 30))
}

// Главная функция
func main() {
	// Инициализация Гоши
	gosha := initGosha()

	// Инициализация генератора случайных чисел
	r := rand.New(rand.NewPCG(
		uint64(time.Now().UnixNano()),
		uint64(time.Now().UnixNano()>>32),
	))

	// Получение темы дня
	theme := getCurrentTheme()

	// Отображение информации
	gosha.displayStatus()
	theme.displayTheme()
	displayMotivation(r)
	displayHistory()
	displayConversionLesson()
	displayLifeLesson()

	// Завершение программы
	fmt.Println("🚀 ПРОДОЛЖАЙ СВОИ ЗАНЯТИЯ, ГОША! ТВОИ 30 ДНЕЙ — ЭТО ПОЛТРИНЫ ПУТИ!")
	fmt.Println("🧠 ЗАВТРА ТЫ СМОЖЕШЬ КОНВЕРТИРОВАТЬ ЛЮБУЮ ПРОБЛЕМУ В ВОЗМОЖНОСТЬ!")
}
