package vendorLogic

import "fmt"

// ShowVendorBenefits показывает преимущества использования vendor
func ShowVendorBenefits() {
    fmt.Println("\n💰 ПРЕИМУЩЕСТВА VENDOR:")

    benefits := []struct {
        title string
        desc  string
    }{
        {
            "🎯 1. Изоляция проекта",
            "Все зависимости в папке проекта, не зависят от глобального GOPATH",
        },
        {
            "🔒 2. Воспроизводимость",
            "Гарантия, что все разработчики используют одинаковые версии",
        },
        {
            "🌐 3. Работа оффлайн",
            "Сборка работает без доступа к интернету и GitHub",
        },
        {
            "🚢 4. Деплой без сюрпризов",
            "На продакшене используются те же зависимости, что и при разработке",
        },
        {
            "📦 5. Контроль версий",
            "Легко увидеть, какие версии библиотек используются",
        },
    }

    for _, b := range benefits {
        fmt.Printf("   %s\n", b.title)
        fmt.Printf("      %s\n", b.desc)
    }
}

// VendorMetaphor возвращает аналогию с легендой дня
func VendorMetaphor() string {
    return `
    💎 АНАЛОГИЯ С ЛЕГЕНДОЙ ДНЯ:

    Гоша положил 600 рублей в копилку → Мы кладём зависимости в vendor/

    Без копилки (без vendor):
    - Деньги (зависимости) разбросаны где-то
    - Нужно идти в банк (интернет) за каждой копейкой
    - Риск: банк закрыт (нет интернета) = нет денег

    С копилкой (с vendor):
    - Все деньги (зависимости) в одном месте
    - Доступны в любой момент
    - Можно пересчитать и проверить
    `
}
