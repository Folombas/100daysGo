package main

import (
	"fmt"
	"time"
)

// 🌫️ ГЛОБАЛЬНЫЕ ПЕРЕМЕННЫЕ С НУЛЕВЫМИ ЗНАЧЕНИЯМИ (состояние Гоши утром)
var (
	energyLevel int     // 0 - полное отсутствие энергии
	motivation  bool    // false - нет мотивации
	mood        string  // "" - пустота в душе
	currentJob  *string // nil - отсутствие работы
	todayOrders int     // 0 - заказов ещё нет
)

func main() {
	// 📅 Сегодняшняя дата
	today := time.Date(2025, time.November, 26, 8, 0, 0, 0, time.Local)
	fmt.Printf("📅 %s, %s\n", today.Weekday(), today.Format("02.01.2006"))
	fmt.Println("❄️ 8:00 УТРА. ЗА ОКНОМ - СЕРАЯ НОЯБРЬСКАЯ ПРОМОЗГЛОСТЬ, +1°C. КВАРТИРА ТЁПЛАЯ, А ДУША ХОЛОДНАЯ...")

	// 🖥️ ГЛОБАЛЬНЫЕ НУЛЕВЫЕ ЗНАЧЕНИЯ (реальность Гоши)
	fmt.Println("🧠 СОСТОЯНИЕ ГОШИ ПРИ ПРОБУЖДЕНИИ (zero values):")
	printVariableState("energyLevel", energyLevel, 0)    // int → 0
	printVariableState("motivation", motivation, false) // bool → false
	printVariableState("mood", mood, "")                 // string → ""
	printVariableState("currentJob", currentJob, nil)    // *string → nil
	printVariableState("todayOrders", todayOrders, 0)   // int → 0
	fmt.Println()

	// 💭 МЫСЛИ В ДЕПРЕССИИ (как неинициализированные переменные)
	fmt.Println("💭 ВНУТРЕННИЙ МОНОЛОГ ПОД ОДЕЯЛОМ:")
	fmt.Println(`"Раньше меня будил продюсер: 'Гоша, запись нового хита! гЕЛИЕ С ШОФЁРОМ ЖДУТ У ПАДИКА, ПОТОРОПИСЬ, ЭМСИ!'`)
	fmt.Println(`Теперь будит только нужда и страх остаться без вкусного обеда...`)
	fmt.Println(`Все нулевые значения: энергия = 0, мотивация = false, настроение = ""...`)
	fmt.Println(`Даже работа = nil. Просто пустота. Как неинициализированная переменная в Go."`)
	fmt.Println()

	time.Sleep(2 * time.Second)
	fmt.Println("⚡ ВНЕЗАПНО: ВСПОМИНАЕТ УРОК ПО GO ИЗ ВЧЕРАШНЕГО ВИДЕО")

	// 💻 ИЗУЧЕНИЕ ZERO VALUES (метафора перезагрузки)
	fmt.Println("💻 УРОК GO В ТЕПЛЕ (вспоминает под одеялом):")
	fmt.Println(`"В Go каждая переменная имеет нулевое значение по умолчанию:`)
	fmt.Println(`   • int = 0`)
	fmt.Println(`   • bool = false`)
	fmt.Println(`   • string = ""`)
	fmt.Println(`   • указатель = nil`)
	fmt.Println(`Но это не конец! Это ЧИСТЫЙ ЛИСТ для будущего кода!"`)
	fmt.Println()

	time.Sleep(2 * time.Second)
	fmt.Println("🔥 ПЕРЕЛОМНЫЙ МОМЕНТ: РЕШЕНИЕ ВСТАТЬ С ПОСТЕЛИ")

	// 🌱 ИНИЦИАЛИЗАЦИЯ ПЕРЕМЕННЫХ (действия Гоши)
	energyLevel = 30                      // Собрался с силами
	motivation = true                     // Вспомнил мечту
	mood = "resolute"                     // Решил действовать
	jobTitle := "Golang Developer"            // Будущая профессия
	currentJob = &jobTitle                // Указал на мечту
	todayOrders = 3                       // Заказы для выживания

	// 💪 ФИЗИЧЕСКИЕ ДЕЙСТВИЯ (метафора инициализации)
	fmt.Printf("🛏️ 8:15: ВСТАЛ С КРОВАТИ (energyLevel = %d)\n", energyLevel)
	fmt.Printf("🚿 8:30: ХОЛОДНЫЙ ДУШ (motivation = %v)\n", motivation)
	fmt.Printf("👕 8:45: ОДЕЛСЯ ПО ПОГОДЕ (mood = \"%s\")\n", mood)
	fmt.Printf("💼 9:00: ВЗЯЛ РЮКЗАК ДЛЯ ЗАКАЗОВ (todayOrders = %d)\n", todayOrders)
	fmt.Printf("🚀 9:05: ВЫШЕЛ НА УЛИЦУ, ДУМАЯ: \"Моя работа — %s\"\n\n", *currentJob)

	// 🌟 МОТИВАЦИОННЫЙ ФИНАЛ
	fmt.Println("🎯 ФИЛОСОФИЯ ZERO VALUES В ЖИЗНИ ГОШИ:")
	fmt.Println(`"Моё утро началось с нулевых значений — как переменные в Go.`)
	fmt.Println(`Но ноль — это не провал! Это возможность наполнить себя новым смыслом.`)
	fmt.Println(`Из 0 энергии → 30% для старта`)
	fmt.Println(`Из false мотивации → true мечты`)
	fmt.Println(`Из пустой строки настроения → \"resolute\" решение`)
	fmt.Println(`Из nil работы → указатель на \"Go Developer\"`)
	fmt.Println(`Даже 3 курьерских заказа = 3 шага к будущей зарплате в $4000!"`)
	fmt.Println()
	fmt.Println("🔥 ГЛАВНЫЙ УРОК ДНЯ:")
	fmt.Println(`"В Go, как и в жизни: нулевые значения — это не пустота.`)
	fmt.Println(`Это ЧИСТЫЙ ЛИСТ, на котором ты пишешь свою новую историю.`)
	fmt.Println(`Сегодня я развозил заказы. Завтра я буду писать код в офисе при +22°C."`)
}

// 📊 Вспомогательная функция для демонстрации нулевых значений
func printVariableState(name string, value interface{}, zeroValue interface{}) {
	// ✅ Проверяем, является ли значение нулевым
	isZero := fmt.Sprintf("%v", value) == fmt.Sprintf("%v", zeroValue)
	status := "✅ НУЛЕВОЕ ЗНАЧЕНИЕ (начало пути)"
	if !isZero {
		status = "🔥 ИНИЦИАЛИЗИРОВАНО (прогресс!)"
	}

	fmt.Printf("   • %s = %v | Тип: %T | Статус: %s\n",
		name, value, value, status)
}
