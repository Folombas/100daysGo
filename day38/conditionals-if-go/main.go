package main

import "fmt"

func main() {
	rainyDay := true      // Дождливый день
	wetShoes := true      // Мокрые ботинки
	hasHope := true       // Есть надежда
	moneyForFood := false // Нет денег на еду

	fmt.Println("Текущие условия:")
	fmt.Printf("Дождь: %v\nМокрые ботинки: %v\nДеньги на еду: %v\n\n",
		rainyDay, wetShoes, moneyForFood)

	// Проверяем базовые условия
	if rainyDay && wetShoes {
		fmt.Println("⚠️  Внимание: Ты промок до нитки под декабрьским дождём!")
	} else {
		fmt.Println("🌤 Погода позволяет сосредоточиться на коде.")
	}

	// Вложенные условия для мотивации
	if moneyForFood {
		fmt.Println("✅ Есть деньги на еду — можно спокойно учиться!")
	} else {
		fmt.Println("💔 Нет денег на еду... Но знания — твой главный капитал.")
		if hasHope {
			fmt.Println("🔥 Однако: Каждая строчка кода — шаг к теплому офису и сухим ботинкам!")
		}
	}

	// Главный вывод дня
	fmt.Println("\nГлавное условие успеха:")
	if hasHope {
		fmt.Println("🌟 ПОБЕДА! Ты читал книгу в метро вместо скроллинга. Это твой superpower!")
	} else {
		fmt.Println("💡 Совет: Включи 'режим надежды' — он всегда true для настоящих кодеров!")
	}
}
