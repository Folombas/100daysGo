# 📦 call-by-value

**Модуль Go, объясняющий принцип "Передача по значению" через историю Гоши**

🚇 *21 декабря 2025: Воскресенье, которое стало рабочим*

## 🎯 Суть концепции

В Go аргументы функций передаются **по значению** (Pass by Value). Это означает, что функция получает **копию** переменной, а не ссылку на неё.
- Изменения внутри функции **не влияют** на исходную переменную.
- Это защищает данные от случайных мутаций (side effects).

> **Метафора:** Твой опыт работы курьером не "перезаписывает" твой потенциал разработчика. Это просто копия времени, отданная работе, пока оригинал (ты сам) остаётся неизменным.

## 📊 Разбор функций

### `calculateEarnings(deliveries int, hourlyRate float64)`
Демонстрирует защиту финансовых данных.

| Действие внутри функции | Влияние на `main` |
|-------------------------|-------------------|
| `hourlyRate` умножается на 0.8 (износ обуви) | **Нет**. В `main` ставка остаётся 400₽ |
| Вычисление заработка на основе копии | Возвращается результат, исходная ставка в безопасности |

### `analyzeEnergyLevel(energy int)`
Демонстрирует защиту состояния.

| Действие внутри функции | Влияние на `main` |
|-------------------------|-------------------|
| `energy -= 20` (усталость от анализа) | **Нет**. В `main` энергия остаётся 30/100 |
| Возврат мотивирующего сообщения | Исходный уровень энергии сохранён для других задач |

### `forecastITCareer(weeksOfStudy int)`
Демонстрирует создание нового значения.

- Мы не меняем прошлое. Мы используем текущие данные (`weeksOfStudy`), чтобы создать **новое** будущее (`salary`, `jobTitle`).
