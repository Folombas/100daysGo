package main

import "fmt"

func demoSummary() {
    fmt.Println("\n3. КРАТКАЯ ШПАРГАЛКА")
    fmt.Println("___________________")
    
    fmt.Println(`
🎯 ОСНОВНЫЕ ПРАВИЛА:

1. & - значит "ДАЙ АДРЕС"
	Пример: &message → адрес message

2. * - значит "ДАТЬ ТО, ЧТО ПО АДРЕСУ"  
   Пример: *pointer → значение по адресу

3. * перед типом - значит "ЭТО УКАЗАТЕЛЬ"
   Пример: var p *string → указатель на строку

4. Если указатель nil - он никуда не указывает

💡 ЗАПОМНИ:
- Указатели ЭКОНОМЯТ память
- Указатели позволяют ИЗМЕНЯть оригиналы
- Указатели - это ПРОСТО АДРЕСА`)

    fmt.Println("\n📚 Пример из кода:")
    fmt.Println("   message := \"Привет\"")
    fmt.Println("   pointer := &message  // сохраняем адрес")
    fmt.Println("   *pointer = \"Пока\"   // меняем оригинал")
    fmt.Println("   fmt.Print(message)   // выведет: Пока")
}
