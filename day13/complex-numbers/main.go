package main

import (
	"fmt"
	"math"
	"math/cmplx"
)

func main() {
	fmt.Println("🔢 Go и комплексные числа: Волны, сигналы и мнимые единицы в реальном коде")
	fmt.Println("📚 Демонстрация основных операций с комплексными числами в Go.")

	// 1. Создание комплексных чисел
	z1 := complex(3, 4)   // 3 + 4i
	z2 := complex(1, -2)  // 1 - 2i

	fmt.Printf("🔹 z1 = %v (типа %T)\n", z1, z1)
	fmt.Printf("🔹 z2 = %v (типа %T)\n\n", z2, z2)

	// 2. Арифметические операции
	fmt.Println("🧮 Арифметические операции:")
	fmt.Printf("🔹 Сложение: z1 + z2 = %v\n", z1+z2)
	fmt.Printf("🔹 Вычитание: z1 - z2 = %v\n", z1-z2)
	fmt.Printf("🔹 Умножение: z1 * z2 = %v\n", z1*z2)
	fmt.Printf("🔹 Деление: z1 / z2 = %v\n\n", z1/z2)

	// 3. Встроенные функции для комплексных чисел
	fmt.Println("🧩 Встроенные функции:")
	fmt.Printf("🔹 Модуль (абсолютное значение): |z1| = %.2f\n", cmplx.Abs(z1))
	fmt.Printf("🔹 Сопряжённое число: z1* = %v\n", cmplx.Conj(z1))
	fmt.Printf("🔹 Фаза (угол): arg(z1) = %.2f рад = %.2f град\n", cmplx.Phase(z1), cmplx.Phase(z1)*180/math.Pi)

	// 4. Возведение в степень и корни
	fmt.Println("\n🔍 Возведение в степень и извлечение корней:")
	z3 := complex(1, 1)
	fmt.Printf("🔹 z3 = %v\n", z3)
	fmt.Printf("🔹 z3^2 = %v\n", cmplx.Pow(z3, 2))
	fmt.Printf("🔹 √z3 = %v\n", cmplx.Sqrt(z3))

	// 5. Экспонента, синус, косинус
	fmt.Println("\n🔬 Тригонометрия и экспонента:")
	fmt.Printf("🔹 e^iπ = %v (должно быть ≈ -1)\n", cmplx.Exp(complex(0, math.Pi)))
	fmt.Printf("🔹 sin(z1) = %v\n", cmplx.Sin(z1))
	fmt.Printf("🔹 cos(z1) = %v\n", cmplx.Cos(z1))

	// 6. Пример: вращение точки на комплексной плоскости
	fmt.Println("\n🔄 Пример: вращение точки на 90 градусов (умножение на i)")
	point := complex(3, 0) // точка (3, 0)
	i := complex(0, 1)     // мнимая единица
	rotated := point * i   // результат: 0 + 3i
	fmt.Printf("🔹 Точка до вращения: %v\n", point)
	fmt.Printf("🔹 Точка после вращения на 90°: %v\n", rotated)

	fmt.Println("\n✅ Программа завершена. Комплексные числа — это мощный инструмент!")
}
