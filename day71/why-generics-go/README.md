# 🎯 Day 71: Why Generics in Go?

## 📖 Описание модуля

Модуль `why-generics-go` объясняет, зачем нужны Generics в языке программирования Go, используя примеры из Легенды Дня 14 января 2026 года.

## 🎭 Легенда Дня

Гоша просыпается в 6:30, делает 3 доставки в снежную погоду, промочив ботинки. В электричке читает бумажную книгу про Docker (подарок на Новый Год) и осознает, что Docker написан на Go. Мокрые ботинки становятся дополнительной мотивацией учить Go!

## 🤔 Зачем нужны Generics?

### Проблемы без Generics:
1. **Дублирование кода** - одинаковые алгоритмы для разных типов
2. **Interface{} с runtime проверками** - потеря типобезопасности
3. **Генерация кода** - зависимость от сторонних утилит
4. **Сложность поддержки** - изменения нужно вносить в нескольких местах

### Решение с Generics:
1. **Типобезопасность** - проверка на этапе компиляции
2. **Переиспользуемость** - один код для многих типов
3. **Производительность** - нет overhead как у interface{}
4. **Читаемость** - явные типы в объявлениях

## 🔧 Примеры из кода

### Без Generics (дублирование):
```go
func SumInts(nums []int) int {
    total := 0
    for _, n := range nums {
        total += n
    }
    return total
}

func SumFloats(nums []float64) float64 {
    total := 0.0
    for _, n := range nums {
        total += n
    }
    return total
}
