package main

import (
	"fmt"
	"math"
	"runtime"
	"unsafe"
)

func main() {
	fmt.Println("🚀 100daysGo: Эпическая Перезагрузка 🚀")
	fmt.Println("══════════════════════════════════════════════════")
	fmt.Printf("👤 Герой: Гоша, 37 лет | СДВГ+ОКР+Социофобия\n")
	fmt.Printf("🎯 Миссия: Превратиться из курьера в Go-разработчика\n")
	fmt.Printf("📚 Сегодня 14 ноября 2025 года мы изучаем тему: Numeric Types - Integers (Signed, Unsigned)\n")
	fmt.Println()

	// Упрёки мамы
	fmt.Println("👵 МАМИНЫ УПРЁКИ:")
	momQuotes := []string{
		"Опять за своим компом сидишь? Может, лучше бы работу искал?",
		"В твоём возрасте нормальные люди уже семью содержат, а не в игры играют!",
		"Говорил бы на английском учил, как все нормальные люди!",
		"И долго ты ещё будешь на моей шее сидеть?",
		"Соседский Коля уже вторую машину купил, а ты...",
		"Может, лучше бы вышел и развёз пару заказов? Хоть какие-то деньги!",
		"Сколько можно сидеть в этих цифрах? Надо работать!",
		"Когда ты уже начнёшь зарабатывать? Уже 38 лет!",
	}
	for _, quote := range momQuotes {
		fmt.Printf("   👵 \"%s\"\n", quote)
	}
	fmt.Println()

	fmt.Println("🔢 ЦЕЛЫЕ ЧИСЛА В GO")
	fmt.Println("====================")

	// Подзаголовок: Знаковые целые числа
	fmt.Println("🎯 ЗНАКОВЫЕ ЦЕЛЫЕ ЧИСЛА (Signed Integers):")
	fmt.Println("   Знаковые типы могут хранить как положительные, так и отрицательные значения")
	fmt.Println()

	// int8
	var int8Var int8 = 127
	fmt.Printf("int8: %d (диапазон: %d до %d, размер: %d байт)\n",
		int8Var, int8(math.MinInt8), int8(math.MaxInt8),
		unsafe.Sizeof(int8Var))

	// int16
	var int16Var int16 = 32767
	fmt.Printf("int16: %d (диапазон: %d до %d, размер: %d байт)\n",
		int16Var, int16(math.MinInt16), int16(math.MaxInt16),
		unsafe.Sizeof(int16Var))

	// int32
	var int32Var int32 = 2147483647
	fmt.Printf("int32: %d (диапазон: %d до %d, размер: %d байт)\n",
		int32Var, int32(math.MinInt32), int32(math.MaxInt32),
		unsafe.Sizeof(int32Var))

	// int64
	var int64Var int64 = 9223372036854775807
	fmt.Printf("int64: %d (диапазон: %d до %d, размер: %d байт)\n",
		int64Var, int64(math.MinInt64), int64(math.MaxInt64),
		unsafe.Sizeof(int64Var))

	// int (архитектурозависимый)
	var intVar int = 2147483647 // или 9223372036854775807 на 64-битной системе
	fmt.Printf("int: %d (архитектурозависимый, размер: %d байт)\n",
		intVar, unsafe.Sizeof(intVar))

	fmt.Println()
	fmt.Println("🎯 БЕЗЗНАКОВЫЕ ЦЕЛЫЕ ЧИСЛА (Unsigned Integers):")
	fmt.Println("   Беззнаковые типы могут хранить только неотрицательные значения (0 и положительные)")
	fmt.Println()

	// uint8 (byte)
	var uint8Var uint8 = 255
	fmt.Printf("uint8 (byte): %d (диапазон: 0 до %d, размер: %d байт)\n",
		uint8Var, uint8(math.MaxUint8),
		unsafe.Sizeof(uint8Var))

	// uint16
	var uint16Var uint16 = 65535
	fmt.Printf("uint16: %d (диапазон: 0 до %d, размер: %d байт)\n",
		uint16Var, uint16(math.MaxUint16),
		unsafe.Sizeof(uint16Var))

	// uint32
	var uint32Var uint32 = 4294967295
	fmt.Printf("uint32: %d (диапазон: 0 до %d, размер: %d байт)\n",
		uint32Var, uint32(math.MaxUint32),
		unsafe.Sizeof(uint32Var))

	// uint64
	var uint64Var uint64 = 18446744073709551615
	fmt.Printf("uint64: %d (диапазон: 0 до %d, размер: %d байт)\n",
		uint64Var, uint64(math.MaxUint64),
		unsafe.Sizeof(uint64Var))

	// uint (архитектурозависимый)
	var uintVar uint = 4294967295 // или 18446744073709551615 на 64-битной системе
	fmt.Printf("uint: %d (архитектурозависимый, размер: %d байт)\n",
		uintVar, unsafe.Sizeof(uintVar))

	fmt.Println()
	fmt.Println("🎯 СПЕЦИАЛЬНЫЕ ТИПЫ:")

	// rune (alias для int32)
	var runeVar rune = 'A'
	fmt.Printf("rune: %c (alias для int32, диапазон: %d до %d, размер: %d байт)\n",
		runeVar, int32(math.MinInt32), int32(math.MaxInt32),
		unsafe.Sizeof(runeVar))

	// byte (alias для uint8)
	var byteVar byte = 'B'
	fmt.Printf("byte: %c (alias для uint8, диапазон: 0 до %d, размер: %d байт)\n",
		byteVar, uint8(math.MaxUint8),
		unsafe.Sizeof(byteVar))

	fmt.Println()
	fmt.Println("🎯 ПРАКТИЧЕСКИЕ ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:")

	// Примеры использования разных типов
	fmt.Printf("Пример использования int8 (температура): %d°C\n", int8(-40))
	fmt.Printf("Пример использования uint8 (процент): %d%%\n", uint8(95))
	fmt.Printf("Пример использования int32 (ID пользователя): %d\n", int32(123456789))
	fmt.Printf("Пример использования uint64 (количество байт): %d\n", uint64(1024*1024*1024))

	// Демонстрация переполнения
	fmt.Println()
	fmt.Println("🎯 ДЕМОНСТРАЦИЯ ПЕРЕПОЛНЕНИЯ:")

	var smallInt uint8 = 255
	fmt.Printf("uint8 перед переполнением: %d\n", smallInt)
	smallInt++
	fmt.Printf("uint8 после переполнения: %d (сброс до 0)\n", smallInt)

	var signedInt int8 = 127
	fmt.Printf("int8 перед переполнением: %d\n", signedInt)
	signedInt++
	fmt.Printf("int8 после переполнения: %d (сброс до -128)\n", signedInt)

	fmt.Println()
	fmt.Println("🎯 АРИФМЕТИЧЕСКИЕ ОПЕРАЦИИ:")

	// Операции с разными типами
	a := int16(100)
	b := int16(25)

	fmt.Printf("a = %d, b = %d\n", a, b)
	fmt.Printf("Сложение: a + b = %d\n", a+b)
	fmt.Printf("Вычитание: a - b = %d\n", a-b)
	fmt.Printf("Умножение: a * b = %d\n", a*b)
	fmt.Printf("Деление: a / b = %d\n", a/b)
	fmt.Printf("Остаток: a %% b = %d\n", a%b)

	// Операции с беззнаковыми
	c := uint32(1000)
	d := uint32(7)
	fmt.Printf("\nБеззнаковые: c = %d, d = %d\n", c, d)
	fmt.Printf("Сложение: c + d = %d\n", c+d)
	fmt.Printf("Деление: c / d = %d\n", c/d)

	fmt.Println()
	fmt.Println("🎯 ПРИВЕДЕНИЕ ТИПОВ:")

	// Примеры приведения
	var signedInt32 int32 = 42
	var unsignedInt32 uint32 = uint32(signedInt32)
	var signedInt64 int64 = int64(signedInt32)

	fmt.Printf("int32 %d -> uint32: %d\n", signedInt32, unsignedInt32)
	fmt.Printf("int32 %d -> int64: %d\n", signedInt32, signedInt64)

	// Попытка приведения отрицательного к беззнаковому
	var negativeInt int32 = -1
	var unsignedFromNegative uint32 = uint32(negativeInt)
	fmt.Printf("int32 %d -> uint32: %d (внимание: неожиданный результат!)\n", negativeInt, unsignedFromNegative)

	fmt.Println()
	fmt.Println("🎯 ИНФОРМАЦИЯ О СИСТЕМЕ:")
	fmt.Printf("Архитектура: %s\n", runtime.GOARCH)
	fmt.Printf("Операционная система: %s\n", runtime.GOOS)
	fmt.Printf("Размер int: %d байт\n", unsafe.Sizeof(int(0)))
	fmt.Printf("Размер uint: %d байт\n", unsafe.Sizeof(uint(0)))

	fmt.Println()
	fmt.Println("🎯 СТАТИСТИКА ПО ТИПАМ:")

	// Статистика по размерам типов
	types := []interface{}{
		int8(0), int16(0), int32(0), int64(0), int(0),
		uint8(0), uint16(0), uint32(0), uint64(0), uint(0),
		rune(0), byte(0),
	}

	for _, t := range types {
		fmt.Printf("Тип: %T, Размер: %d байт\n", t, unsafe.Sizeof(t))
	}

	fmt.Println()
	fmt.Println("══════════════════════════════════════════════════")
	fmt.Println("💡 Напутствие: Знание диапазонов числовых типов - ключ к эффективному программированию!")
	fmt.Println("   Выбирай правильный тип данных в зависимости от задачи!")
	fmt.Println("   Ты не просто учишь Go - ты переписываешь свою судьбу!")
	fmt.Println("   Мама гордится твоим упорством, скоро будет гордиться и зарплатой!")
	fmt.Println()

	// Последний упрёк мамы
	fmt.Println("👵 ФИНАЛЬНЫЙ УПРЁК МАМЫ:")
	fmt.Printf("   👵 \"Ну хоть что-то полезное из тебя выйдет, сидишь, как дед, в своих цифрах...\"\n")
}
