# Гоша - Вечный Двигатель Прогресса (Structs: Embedding Structs)

## ❄️ Описание дня

**9 декабря 2025 года.** Сегодня Гоша еле-еле встал с постели в час дня. Глубокая зимняя депрессия накрыла с головой. Проездной билет закончился, кварплату оплатил, еле наскребя деньги. На улице лютый холод, а курьерская оплата такая же, как и летом — копеечная. Нет денег на нормальную зимнюю одежду, на отопление. Но в сердце теплится надежда: через 64 дня он станет Go-разработчиком и забудет, что значит мерзнуть из-за денег.

## 🧩 Тема дня: Structs: Embedding Structs

В Go можно вкладывать одну структуру в другую без явного имени поля — это называется **embedding**. Это мощный паттерн, позволяющий создавать иерархии и переиспользовать код:

```go
// Базовая структура
type FinancialState struct {
    HasPassTicket bool
    CurrentCash   float64
}

// Другая базовая структура
type ProgrammingProgress struct {
    DaysCompleted int
    Confidence    int
}

// Главная структура с embedding
type Gosha struct {
    Name  string
    Age   int
    FinancialState     // embedding без имени!
    ProgrammingProgress // embedding без имени!
}

// Теперь можно вызывать методы напрямую:
gosha.Display() // вызовет метод из FinancialState или ProgrammingProgress
