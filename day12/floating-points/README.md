# Floating Points в Go — числа с плавающей точкой

## 🎯 Назначение

Эта программа демонстрирует работу с **числами с плавающей точкой** (`float32`, `float64`) в языке Go. Она показывает:

- Типы `float32` и `float64`
- Диапазоны и точность
- Арифметические операции
- Особенности сравнения чисел
- Приведение типов
- Специальные значения (бесконечность, NaN)

## 🧠 Особенности

- `float32` — 32-битное число с плавающей точкой (меньше точность, меньше памяти)
- `float64` — 64-битное число с плавающей точкой (выше точность, больше памяти)
- **Важно**: не сравнивайте `float` напрямую через `==` из-за погрешностей представления чисел

## 🧪 Пример запуска

```bash
go run main.go
