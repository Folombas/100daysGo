package main

import (
	"fmt"
	"math"
	"runtime"
	"unsafe"
)

func main() {
	fmt.Println("🚀 100daysGo: Reboot — Day12 🚀")
	fmt.Println("══════════════════════════════════════════════════")
	fmt.Printf("👤 Герой: Гоша, 38 лет | СДВГ+ОКР+Социофобия\n")
	fmt.Printf("🎯 Миссия: Превратиться из курьера в Go-разработчика\n")
	fmt.Printf("📚 Тема: Numeric Types - Floating Points\n")
	fmt.Printf("📅 Дата: %s\n", "16 ноября 2025")
	fmt.Printf("❄️ Погода: Снег, холодный ветер, снежная каша, клиенты\n")
	fmt.Println()

	// Упрёки мамы
	fmt.Println("👵 МАМИНЫ УПРЁКИ:")
	momQuotes := []string{
		"Опять сидишь в компе? Может, лучше бы в магазин сходил, молока купил?",
		"В твоём возрасте нормальные люди уже детей кормят, а ты всё в цифрах сидишь!",
		"Вот соседский Коля — у него уже третий ребёнок, а ты всё курьерничаешь!",
		"Сколько можно в этих играх-то сидеть? Работать надо!",
		"Может, лучше бы в спортзал сходил? А не всё в компьютере ковырялся!",
		"Ты бы хоть раз в жизни нормально заработал, а не эти копейки на доставке!",
	}
	for _, quote := range momQuotes {
		fmt.Printf("   👵 \"%s\"\n", quote)
	}
	fmt.Println()

	// Клиентские упрёки
	fmt.Println("📞 КЛИЕНТСКИЕ УПРЁКИ:")
	clientQuotes := []string{
		"Что вы тупите? В заказе написан код двери от подъезда!",
		"Да, лифт не работает, ну и чё? Поднимайтесь пешком на 28-й этаж, быстрее!",
		"Да, на этаже тоже дверь с домофоном. Еле дозвонился из холла? Ну дозвонился же! Держи код ещё от одной двери!",
		"Ну вы тормоз! Я уже 5 минут жду! Какой ещё гололёд?",
		"Почему вы без перчаток? Вы же ехали в этом, как его, метро. У вас грязные руки!",
		"Вы улыбнулись не во весь рот! Я подам жалобу!",
	}
	for _, quote := range clientQuotes {
		fmt.Printf("   📞 \"%s\"\n", quote)
	}
	fmt.Println()

	fmt.Println("🔢 ЧИСЛА С ПЛАВАЮЩЕЙ ТОЧКОЙ В GO")
	fmt.Println("=================================")

	// float32
	var float32Var float32 = 3.14159265358979323846
	fmt.Printf("float32: %.10f (диапазон: ~%e до ~%e, размер: %d байт)\n",
		float32Var, float32(math.SmallestNonzeroFloat32), float32(math.MaxFloat32),
		unsafe.Sizeof(float32Var))

	// float64
	var float64Var float64 = 3.14159265358979323846
	fmt.Printf("float64: %.20f (диапазон: ~%e до ~%e, размер: %d байт)\n",
		float64Var, math.SmallestNonzeroFloat64, math.MaxFloat64,
		unsafe.Sizeof(float64Var))

	// Тип по умолчанию
	var defaultFloat = 3.14159265358979323846
	fmt.Printf("float (без указания типа): %.20f (Go сам выбирает float64)\n",
		defaultFloat)

	fmt.Println()
	fmt.Println("🎯 ПРАКТИЧЕСКИЕ ПРИМЕРЫ:")

	// Примеры использования
	price := float64(123.45)
	fmt.Printf("Цена товара: %.2f руб.\n", price)

	temperature := float32(-25.7)
	fmt.Printf("Температура: %.1f°C\n", temperature)

	distance := float64(1234.567890123456)
	fmt.Printf("Расстояние: %.10f км\n", distance)

	fmt.Println()
	fmt.Println("🎯 АРИФМЕТИЧЕСКИЕ ОПЕРАЦИИ:")

	a := float64(10.5)
	b := float64(3.2)

	fmt.Printf("a = %.2f, b = %.2f\n", a, b)
	fmt.Printf("Сложение: a + b = %.2f\n", a+b)
	fmt.Printf("Вычитание: a - b = %.2f\n", a-b)
	fmt.Printf("Умножение: a * b = %.2f\n", a*b)
	fmt.Printf("Деление: a / b = %.2f\n", a/b)

	fmt.Println()
	fmt.Println("🎯 ОПАСНОСТИ ПРИ СРАВНЕНИИ:")

	x := float64(0.1)
	y := float64(0.2)
	sum := x + y

	fmt.Printf("0.1 + 0.2 = %.20f\n", sum)
	fmt.Printf("0.1 + 0.2 == 0.3 ? %t\n", sum == 0.3) // false!
	fmt.Printf("Разница: %.20f\n", math.Abs(sum-0.3))

	fmt.Println()
	fmt.Println("🎯 ПРИВЕДЕНИЕ ТИПОВ:")

	var intVal int = 42
	var floatFromInt float64 = float64(intVal)
	fmt.Printf("int %d -> float64: %.2f\n", intVal, floatFromInt)

	var floatVal float64 = 42.7
	var intFromFloat int = int(floatVal)
	fmt.Printf("float64 %.1f -> int: %d (дробная часть отброшена!)\n", floatVal, intFromFloat)

	fmt.Println()
	fmt.Println("🎯 СПЕЦИАЛЬНЫЕ ЗНАЧЕНИЯ:")

	zero := float64(0.0) // теперь используется
	positiveInf := math.Inf(1)
	negativeInf := math.Inf(-1)
	nan := math.NaN()

	fmt.Printf("Ноль: %f\n", zero) // используем переменную
	fmt.Printf("Положительная бесконечность: %f\n", positiveInf)
	fmt.Printf("Отрицательная бесконечность: %f\n", negativeInf)
	fmt.Printf("Не-число (NaN): %f\n", nan)
	fmt.Printf("Является ли NaN: %t\n", math.IsNaN(nan))

	// Деление нуля на ноль — безопасно, но возвращает NaN
	result := zero / zero
	fmt.Printf("0.0 / 0.0 = %f (NaN)\n", result)

	fmt.Println()
	fmt.Println("🎯 ИНФОРМАЦИЯ О СИСТЕМЕ:")
	fmt.Printf("Архитектура: %s\n", runtime.GOARCH)
	fmt.Printf("Размер float32: %d байт\n", unsafe.Sizeof(float32(0)))
	fmt.Printf("Размер float64: %d байт\n", unsafe.Sizeof(float64(0)))

	fmt.Println()
	fmt.Println("🎯 СТАТИСТИКА ПО ТИПАМ:")

	types := []interface{}{
		float32(0), float64(0),
	}

	for _, t := range types {
		fmt.Printf("Тип: %T, Размер: %d байт\n", t, unsafe.Sizeof(t))
	}

	fmt.Println()
	fmt.Println("══════════════════════════════════════════════════")
	fmt.Println("💡 Знай особенности чисел с плавающей точкой!")
	fmt.Println("   Не сравнивай float напрямую на равенство!")
	fmt.Println("   Используй float64 для высокой точности!")
	fmt.Println("   Ты не просто учишь Go — ты переписываешь свою судьбу!")
	fmt.Println("   Мама гордится твоим упорством, скоро будет гордиться и зарплатой!")
	fmt.Println()

	// Финальный упрёк мамы
	fmt.Println("👵 ФИНАЛЬНЫЙ УПРЁК МАМЫ:")
	fmt.Printf("   👵 \"Ну хоть что-то полезное из тебя выйдет, сидишь, как дед, в своих цифрах...\"\n")
}
