# Day 91: Контекст, Код и Концентрация

## 🎯 Миссия модуля
Понять, как писать вежливые, управляемые и предсказуемые программы на Go с помощью пакета `context`.

## 📁 Структура
- `main.go` — лаборатория, демонстрирующая отмену операций по таймауту
- `go.mod` — файл модуля Go

## 🧪 Что делает программа?
1. Создает контекст с жестким лимитом в **3 секунды**
2. Запускает 3 "тяжелых" API-запроса с разным временем выполнения (1-7 секунд)
3. Автоматически отменяет все операции при превышении лимита
4. Наглядно показывает, как `ctx.Done()` сигнализирует о необходимости завершения

## 🎮 Геймификация задачи
- **Уровень 1**: Запусти программу 5 раз. Зафиксируй, сколько раз все 3 API успели отработать.
- **Уровень 2**: Измени таймаут в коде с 3 на 5 секунд. Насколько улучшилась статистика?
- **Уровень Босса**: Перепиши программу, чтобы каждый API запрос имел свой уникальный контекст с разными таймаутами.

## 📚 Ключевые концепции
- `context.WithTimeout()` — создание контекста с дедлайном
- `ctx.Done()` — канал для отслеживания отмены
- `ctx.Err()` — получение причины отмены (дедлайн/отмена)
- `defer cancel()` — гарантированное освобождение ресурсов

## 💡 Зачем это в реальности?
Контексты — фундамент для:
- Отмены HTTP-запросов пользователем
- Ограничения времени на внешние API-вызовы
- Graceful shutdown серверов
- Распространения метаданных (ID запроса, логин) по цепочке вызовов

## 🧠 Философия дня
Твой код должен быть таким же дисциплинированным, как и ты сам. Четкие дедлайны, реакция на изменения, завершение начатого — это навыки, которые тренируются и в программировании, и в жизни.
