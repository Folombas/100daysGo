# algorithm-sieve-eratosthenes

> **Алгоритм, написанный в движении**  
> Этот код родился не в тихой комнате с двумя мониторами, а в гулком салоне городского автобуса. Пока Гоша ехал по своему маршруту, на его смартфоне Honor под управлением Ubuntu в Termux оживала древняя математическая идея.

## 🚌 Легенда

**Место действия:** Общественный транспорт, маршрут № ...  
**Время:** Час пик.  
**Инструмент:** Смартфон Honor, Termux, мобильная клавиатура.  
**Задача:** Погрузиться в челлендж `#100daysGo` и реализовать один из самых элегантных алгоритмов в истории — **Решето Эратосфена**.

Философия этого модуля проста: мощные идеи не требуют тишины кабинета. Они могут родиться в самой гуще жизни, между остановками «Центральный рынок» и «Парк Победы».

## 🔢 Что такое Решето Эратосфена?

**Решето Эратосфена** — это древний алгоритм для нахождения всех **простых чисел** до заданного целого числа `n`, приписываемый греческому математику **Эратосфену Киренскому** (III век до н. э.).

**Простое число** — это натуральное число больше 1, которое имеет ровно два делителя: 1 и само себя (например, 2, 3, 5, 7, 11...).

### Красота идеи
Вместо того чтобы проверять делимость каждого числа по отдельности (что очень медленно), алгоритм действует как настоящее решето:
1.  **Записываем** все числа от 2 до `n`.
2.  **Берём первое незачёркнутое число** — оно простое.
3.  **Вычёркиваем все его кратные** — они составные.
4.  **Переходим к следующему незачёркнутому числу** и повторяем шаг 3.
5.  Процесс продолжается, пока не будут рассмотрены все числа до `√n`.

**Ключевая оптимизация:** Достаточно проверять кратные чисел, не превышающих квадратный корень из `n`, потому что любое составное число `n` обязательно имеет хотя бы один простой делитель ≤ √`n`.

## 🧠 Как работает наш код?

Модуль `algorithm-sieve-eratosthenes` содержит оптимизированную реализацию алгоритма на Go, созданную «в полевых условиях».

### Основная функция: `optimizedSieve(limit int) []int`
- **Вход:** Целое число `limit` (верхняя граница поиска).
- **Выход:** Срез (slice) целых чисел, содержащий все простые числа от 2 до `limit`.
- **Логика работы:**
    1. Создаётся булев массив `isComposite`, где индекс представляет число, а значение `true` означает, что число составное.
    2. Внешний цикл проходит от `i = 2` до `i*i ≤ limit`.
    3. Если число `i` не отмечено как составное (`!isComposite[i]`), оно считается простым.
    4. Внутренний цикл вычёркивает все кратные `i`, начиная с `i*i`.
    5. В конце собираются все индексы, оставшиеся `false`.

### Дополнительные возможности
- **Аргументы командной строки:** Можно задать свой лимит при запуске: `go run main.go 100`.
- **Бенчмарк-режим:** При больших лимитах автоматически запускается замер производительности.
- **Устойчивость к вводу:** Есть базовая валидация аргументов.

## 🚀 Использование

### Клонирование и запуск
```bash
# 1. Перейдите в ваш рабочий каталог Go
cd ~/go/src/your-project/

# 2. Клонируйте модуль (пример)
git clone <your-repo-url> algorithm-sieve-eratosthenes
cd algorithm-sieve-eratosthenes

# 3. Запустите с лимитом по умолчанию (15)
go run main.go

# 4. Запустите с собственным лимитом (например, 50)
go run main.go 50
	
