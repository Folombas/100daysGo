# Day 99: Race Detection — охота на гонки данных в Go

## 📌 Название модуля
**`race-detector`** — детектор состояний гонки на примере подсчёта дней без CapCut.

## 🎯 Описание
Этот модуль демонстрирует классическую **гонку данных (race condition)** в конкурентных программах на Go и показывает, как её исправить с помощью мьютексов и атомарных операций.
Программа подсчитывает количество дней, прошедших с 18 января 2026 (день удаления CapCut), и в трёх параллельных сценариях пытается увеличить счётчик в 10 горутинах.

## 🧠 Чему вы научитесь?
- Запускать **детектор гонок Go** (`go run -race`).
- Понимать, почему возникает недетерминированное поведение.
- Исправлять гонки с помощью `sync.Mutex`.
- Использовать `atomic` для простых счётчиков.
- Анализировать вывод детектора и находить строки с проблемой.

## 🚀 Запуск программы

```bash
# Без детектора гонок (гонка останется скрытой)
go run main.go

# С детектором гонок — увидите предупреждение
go run -race main.go
