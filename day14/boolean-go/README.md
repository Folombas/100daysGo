# 🎭 Bool-Герой: Как курьер превратил `true` и `false` в путь к Go-мечте

Добро пожаловать в **Day14** нашего эпического челленджа **“100aysG0: Reboot”**! Сегодня мы изучаем **boolean-типы** в Go через драматичную историю о том, как курьер с СДВГ и ОКР превращает `true` и `false` в путь к своей мечте.

---

## 📖 Сюжет программы

Эта tiny-программа рассказывает историю **Гоши, 37-летнего курьера с СДВГ и ОКР**, который:
- Слушает колкие упрёки мамы: *"Опять за компом сидишь? Соседский Коля уже вторую машину купил!"*
- Борется с усталостью от 2-3 ежедневных доставок
- Сомневается в своих силах (уровень стресса 100/100)
- Но находит мотивацию в изучении Go

Через **boolean-логику** (`&&`, `||`, `!`) Гоша принимает судьбоносное решение:
→ `isMotivated = true`
→ `isLearning = true`
→ `isCourier = false` (в будущем!)

Программа показывает, как **простые true/false** могут изменить жизнь:
```go
if hasGolangDream && !isMotivated {
    isMotivated = true // Решающий момент!
}
