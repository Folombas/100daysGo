package main

import (
	"fmt"
	"time"
)

func main() {
	fmt.Println("🎭 Bool-Гуру: Как доставщик еды превратил `true` и `false` в путь к Go-мечте")
	fmt.Println("══════════════════════════════════════════════════════════════════════════════")

	// Персонаж: курьер, мечтающий стать Go-программистом
	name := "Гоша"
	age := 37
	isCourier := true
	isProgrammer := false
	hasGolangDream := true
	isTiredFromDeliveries := true
	isMotivated := false
	isLearning := false
	stressLevel := 100
	confidenceLevel := 0

	fmt.Printf("👤 %s, %d лет | СДВГ + ОКР + Социофобия\n", name, age)
	fmt.Printf("📦 Работает курьером: %t\n", isCourier)
	fmt.Printf("💻 Уже программист? %t\n", isProgrammer)
	fmt.Printf("💭 Мечтает стать Go-разработчиком: %t\n", hasGolangDream)
	fmt.Printf("😩 Устал от доставок: %t\n", isTiredFromDeliveries)
	fmt.Printf("🧠 Уровень стресса: %d/100\n", stressLevel)
	fmt.Printf("💪 Уровень уверенности: %d/100\n", confidenceLevel)
	fmt.Println()

	// Мамкин упрёк
	momQuote := "Опять за компом сидишь? Может, лучше бы в магазин сходил? Соседский Коля уже вторую машину купил, а ты всё курьерничаешь!"
	fmt.Printf("👵 Мама кричит из кухни: \"%s\"\n", momQuote)
	fmt.Println()

	time.Sleep(2 * time.Second)
	fmt.Println("💭 Гоша думает: \"Мам, я не просто сижу. Я изучаю boolean-логику!\"")
	fmt.Println()

	// Принятие решения
	if hasGolangDream && !isMotivated {
		fmt.Println("🔄 Гоша решает: \"Достаточно быть курьером. Пора изменить isMotivated на true!\"")
		isMotivated = true
		fmt.Printf("✅ isMotivated теперь: %t\n", isMotivated)
	}
	fmt.Println()

	// Начало обучения
	if isMotivated && hasGolangDream {
		fmt.Println("📚 Гоша открывает учебник по Go и начинает учиться!")
		isLearning = true
		fmt.Printf("🎯 isLearning = %t\n", isLearning)

		// Снижение стресса и рост уверенности
		stressLevel -= 30
		confidenceLevel += 25
		fmt.Printf("🧠 Уровень стресса: %d/100\n", stressLevel)
		fmt.Printf("💪 Уровень уверенности: %d/100\n", confidenceLevel)
	}
	fmt.Println()

	// Boolean-логика в жизни
	fmt.Println("🧮 Boolean-логика в реальной жизни Гоши:")
	fmt.Printf("  Курьер И Мечтает о Go? %t\n", isCourier && hasGolangDream)
	fmt.Printf("  Курьер ИЛИ Программист? %t\n", isCourier || isProgrammer)
	fmt.Printf("  Мечтает И Не сдался? %t\n", hasGolangDream && !isMotivated)
	fmt.Printf("  Устал И Не мотивирован? %t\n", isTiredFromDeliveries && !isMotivated)
	fmt.Printf("  Учиться ИЛИ Продолжать доставлять? %t\n", isLearning || isCourier)
	fmt.Println()

	// Сравнение: успех vs. усталость
	todaySuccess := 1
	todayDeliveries := 50
	fmt.Println("📊 Сравнение сегодняшних результатов:")
	fmt.Printf("  Успех > Доставок? %t (1 успех против 50 доставок)\n", todaySuccess > todayDeliveries)
	fmt.Printf("  Уверенность > Стресс? %t (%d > %d)\n", confidenceLevel > stressLevel, confidenceLevel, stressLevel)
	fmt.Println()

	// Мораль истории
	fmt.Println("🌟 Мораль истории:")
	fmt.Println("Boolean — это не просто true или false. Это выбор:")
	fmt.Println("  false = продолжать быть курьером с маминым недовольством")
	fmt.Println("  true  = стать Go-программистом, преодолев стресс и сомнения")
	fmt.Println()

	time.Sleep(2 * time.Second)
	fmt.Println("💥 Внезапно раздаётся звонок в дверь — заказ на доставку пельменей!")
	fmt.Println("😤 Гоша вздыхает, но помнит: \"Терпение, Гофер. Каждый день приближает к мечте.\"")
	fmt.Println()

	time.Sleep(3 * time.Second)
	fmt.Println("🚀 Финал истории:")
	fmt.Println("Гоша заканчивает доставку, возвращается домой и снова открывает учебник по Go.")
	fmt.Println("Его внутренний монолог: \"Сегодня я понял разницу между && и ||. Завтра — интерфейсы. Через год — Senior Go Developer!\"")
	fmt.Println()

	fmt.Println("🏆 Эпилог:")
	isProgrammer = true
	isCourier = false
	momQuote = "Ну наконец-то! Когда уже купишь мне дачу на свои программистские деньги?"
	fmt.Printf("💻 isProgrammer = %t | 📦 isCourier = %t\n", isProgrammer, isCourier)
	fmt.Printf("👵 Новая фраза матушки: \"%s\"\n", momQuote)
	fmt.Println()

	fmt.Println("💡 Напутствие от наставника:")
	fmt.Println("Помни: каждый boolean-выбор ведёт к изменению. Сегодня ты выбираешь isLearning = true.")
	fmt.Println("Завтра мир выберет тебя как Go-разработчика. Не сдавайся!")
	fmt.Println("══════════════════════════════════════════════════════════════════════════════")
}
