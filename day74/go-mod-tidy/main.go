package main

import (
    "fmt"
    "os"
    
    "github.com/fatih/color"
    "rsc.io/quote"
)

func main() {
    // Анализ "заказов" (зависимостей)
    fmt.Println("🔍 Анализирую зависимости проекта...")
    
    // Используемые зависимости (полезные "заказы")
    green := color.New(color.FgGreen).SprintFunc()
    fmt.Printf("Используется: %s\n", green("github.com/fatih/color"))
    fmt.Printf("Цитата дня: %s\n", quote.Go())
    
    // "Отменённый заказ" (неиспользуемая зависимость)
    // github.com/gorilla/mux импортирован в go.mod, но не используется в коде
    yellow := color.New(color.FgYellow).SprintFunc()
    fmt.Printf("\n⚠  Обнаружен неиспользуемый модуль: %s\n", yellow("github.com/gorilla/mux"))
    fmt.Println("   (Как тот заказ с ботинками, который Гоша отменил по телефону)")
    
    // "Оптимизированный маршрут" (после go mod tidy)
    red := color.New(color.FgRed, color.Bold).SprintFunc()
    fmt.Printf("\n🚀 %s\n", red("ВЫПОЛНЯЕМ ОПТИМИЗАЦИЮ МАРШРУТА!"))
    fmt.Println("   Запускаем: go mod tidy")
    fmt.Println("   Это удалит неиспользуемые зависимости и упорядочит go.mod")
    
    fmt.Println("\n📦 Текущие зависимости в go.mod:")
    data, _ := os.ReadFile("go.mod")
    fmt.Println(string(data))
}
