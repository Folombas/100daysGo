package main

import (
	"bufio"
	"fmt"
	"os"
	"strings"
)

func main() {
	// Ваши сегодняшние условия (12 декабря 2025)
	freezing := true          // Минусовая температура
	oldShoes := "Freccia" // Старые кожаные туфли
	icyRoads := true          // Гололёд на улице
	readBook := true          // Читал книгу в транспорте
	pascalExamples := true    // Примеры на Паскале

	fmt.Printf("❄️  УТРО 12 ДЕКАБРЯ 2025: %s\n",
		func() string {
			if freezing && icyRoads {
				return "Леденящая комбинация: гололёд + минусовая температура!"
			}
			return "Обычный зимний день"
		}())

	fmt.Printf("👞  Обувь: ")
	if oldShoes == "Freccia" {
		fmt.Println("Твои легендарные 15-летние Freccia — не промокают, но морозят пальцы!")
	} else {
		fmt.Println("Современная зимняя обувь — тепло и сухо")
	}

	// Главное условие дня
	if readBook {
		fmt.Println("\n📖  В ТРАНСПОРТЕ:")
		if pascalExamples {
			fmt.Println("✅ Ты читал книгу по Паскалю, но ДУМАЛ на Go!")
			fmt.Println("💡 Переменные, циклы, условия — это универсально. Синтаксис не важен!")
		} else {
			fmt.Println("⚠️ Сегодня только теория без примеров...")
		}
	} else {
		fmt.Println("\n📱  В ТРАНСПОРТЕ: Скроллил соцсети. Не сегодня.")
	}

	// Интерактив: проверяем твоё решение
	fmt.Println("\n❓ Спроси себя:")
	checkDecision()
}

// Функция для проверки ежедневного решения
func checkDecision() {
	fmt.Print("   Читал ли я сегодня книгу вместо скроллинга? (да/нет): ")
	reader := bufio.NewReader(os.Stdin)
	answer, _ := reader.ReadString('\n')
	answer = strings.TrimSpace(strings.ToLower(answer))

	if answer == "да" || answer == "д" {
		fmt.Println("\n🔥 СУПЕР! Каждая страница — шаг от курьера к разработчику.")
		fmt.Println("🚀 Через год ты будешь смеяться над этими 15-летними туфлями из своего тёплого офиса!")
	} else {
		fmt.Println("\n💔 Не сегодня. Но завтра — новый шанс.")
		fmt.Println("💡 Совет: Положи книгу на главный экран телефона. Пусть будет видна чаще TikTok.")
	}
}


